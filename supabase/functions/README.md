# Edge Functions

Keep this directory empty until the project actually needs Supabase Edge Functions.

Do not put backend business logic here by default. The main application backend should live outside Supabase.
