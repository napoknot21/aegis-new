# Migrations

This directory should contain the official database history.

Recommended naming:

- `YYYYMMDDHHMMSS_init_core_schema.sql`
- `YYYYMMDDHHMMSS_add_memberships.sql`
- `YYYYMMDDHHMMSS_enable_rls.sql`

Only committed, reviewed schema changes belong here.
