# Database Tests

Use `supabase test db` to run pgTAP tests against the local database.

Use this directory for SQL tests around:

- row level security
- grants and roles
- critical constraints
- key business workflows

Keep tests close to the schema changes they validate.
